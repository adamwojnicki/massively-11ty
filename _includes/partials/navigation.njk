<!-- Nav -->
<nav id="nav">
    <ul class="links">
        {% for item in collections.navigation %}
            <li {% if item.url==page.url %}class="active" {% endif %}><a href="{{ item.url }}">{{ item.data.navigation.title }}</a></li>
        {% endfor %}
    </ul>

    <ul class="icons">
        {%  for item in contactInfo.socialMedia %}
            <li><a href="{{ item.url }}" class="icon brands fa-{{ item.name }}"><span class="label">{{ item.name }}</span></a></li>
        {% endfor %}
    </ul>
</nav>